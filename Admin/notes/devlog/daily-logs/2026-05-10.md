# 📅 Daily Log - 2026-05-10

**Project:** KetoCakR Mobile  
**Status:** v0.9.x → v1.0.0  
**Focus:** Bilingual Support (BG/EN) — Localization fixes + Base Recipes Migration

---

## ✅ Завършено днес

### 🌐 Localization Fixes

- [x] **StepsModeToggle.tsx** — Заменени hardcoded BG етикети (`'Текст'`, `'Снимки'`, `'Готвя'`) с `t()` ключове. Проблем: `MODES` масивът е на module level (не може hooks). Решение: премахнат `label` от масива, добавена `modeLabel()` функция вътре в компонента.
- [x] **i18n/bg.ts + en.ts** — Добавени 3 нови ключа в `cookingMode`:
  - `textOnly`: `'Текст'` / `'Text'`
  - `imagesText`: `'Снимки'` / `'Photos'`
  - `cooking`: `'Готвя'` / `'Cooking'`
- [x] **lab-notes/[category]/index.tsx** — Header title не реагираше на смяна на език (използваше `catInfo.title_bg` директно вместо `t()`). Фиксирано чрез `t('labNotes.categories.*')` с fallback.

### 🔤 Base Recipes Bilingual Migration

- [x] **recipe-detail/[id].tsx (line ~210)** — `name: br.name` → `name: language === 'en' ? (br.name_en || br.name) : br.name`
- [x] **user-recipe/[id].tsx (line ~179)** — Същата промяна. Тази screen използва `select('*')`, което автоматично включва `name_en` след добавяне на колоната в БД.
- [x] **SQL UPDATE script** — Генериран скрипт за 9 рецепти без `name_en`:
  - Sliced Almonds, Swiss Meringue, Forest Berries, Tiramisu
  - Fresh Strawberries (x2), Fresh Raspberries, Caramel, Whole Walnuts

---

## 🔄 В процес / Предстои

- [ ] **Изпълни SQL UPDATE** за 9-те рецепти без `name_en` в Supabase
- [ ] **description_en за base_recipes** — Нужно е да се провери дали `description` полето изобщо се показва в мобилното (засега изглежда, че НЕ се използва — само step descriptions и `custom_intro_text_bg/en` от ready_recipes)
- [ ] **step_description_en** — Проверка колко стъпки нямат EN превод; попълване с AI translation
- [ ] **Тест** — Смяна на език на EN в мобилното и потвърждение, че всички имена на компоненти се показват на английски

---

## 🔍 Анализ / Открития

- **CookingModeComponentSelector** и **StepInstructions** — НЕ се нуждаят от промени. Получават вече локализирани данни от `[id].tsx` (transformation layer). Компонентите само рендерират `comp.name` и `step.description` като string.
- **Step descriptions** — Вече напълно локализирани в data layer (`[id].tsx` lines 282–284 и `user-recipe/[id].tsx` lines 232–237). `step_description_en || step_description || step_description_bg` fallback верига.
- **87 от ~96 base recipes** вече имат `name_en` попълнено — само 9 липсваха.

---

## 🐛 Наткнати проблеми

- **MODES на module level** — Не може да се използва `useTranslation()` хук извън компонент. Решено с `modeLabel()` функция вътре в компонента.
- **Lab notes header** — `catInfo.title_bg` не реагира на Zustand store промени. `t()` реагира правилно.
- **Дублирани BG имена** — `Свежи ягоди` се появява 2 пъти с различни ID-та; `Шоколадов ганаш` — 3 пъти. SQL UPDATE по ID е задължителен (не по name).

---

## 💡 Бележки

- `description_en` за base_recipes вероятно не е нужно в мобилното — да се потвърди преди да се пише превод.
- За `step_description_en` — приоритет е да се попълни БД, кодът вече е готов.
- `user-recipe/[id].tsx` използва `select('*')` — всяка нова колона в БД се включва автоматично без промяна на заявката.

---

## 🎯 Следваща сесия

- [ ] Изпълни SQL UPDATE за `name_en` (9 рецепти)
- [ ] Провери и попълни `step_description_en` — query за липсващи EN стъпки
- [ ] Тест на language switching в Expo Go (EN режим)
- [ ] Реши дали `description_en` за base_recipes е нужно

---

*Generated: 2026-05-10 | Session: Bilingual Migration*
